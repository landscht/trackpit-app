import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.trackpit.app',
  appName: 'trackpit',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
